# Notebooks

Thư mục dành cho phân tích khám phá ở các giai đoạn sau. Logic mô phỏng chính
phải nằm trong package `wsn_sim` để có thể kiểm thử và tái sử dụng; notebook
không phải nguồn triển khai chính.
